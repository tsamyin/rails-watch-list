<h1>Lists</h1>

<ol><% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list) %></li>
<% end %></ol>

<%= link_to 'Add a list', new_list_path %>

<%= cl_image_tag("tey3vzqetc4pnzwbv8lt",
      width: 400, height: 300, crop: :fill) %>
